{
  "packages": {
    "compiler": {
      "source": "https://github.com/vnmakarov/mir",
      "ref": "master",
      "commit": null,
      "vendored_at": null,
      "path": "packages/compiler",
      "note": "commit unknown: packages/compiler was moved here from a pre-existing src/ vendor copy with no git history to read the pinned commit from. Run `python3 scripts/update-compiler.py` to sync to current upstream master and populate this entry."
    },
    "toml": {
      "source": "https://github.com/aeronavery/zig-toml",
      "ref": "main",
      "commit": null,
      "vendored_at": null,
      "path": "vendor/toml",
      "note": "Minimal TOML 1.0 parser for mc.toml config loading. Run `python3 scripts/update-toml.py` to re-vendor."
    }
  }
}
